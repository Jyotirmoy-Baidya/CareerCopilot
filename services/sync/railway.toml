[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/service.Dockerfile"
buildArgs = { SERVICE = "sync" }

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
